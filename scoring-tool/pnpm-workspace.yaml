packages:
  - "application"
  - "scoring-tool"
  # ※もし "server" や "e2e" などのフォルダもプロジェクト直下にあれば、同じように追加してください。
  # 分からなければ、単に - "*" （すべてのフォルダを対象にする）と書くこともできます。

allowBuilds:
  esbuild: true
patchedDependencies:
  lighthouse@12.8.2: patches/lighthouse@12.8.2.patch
saveExact: true